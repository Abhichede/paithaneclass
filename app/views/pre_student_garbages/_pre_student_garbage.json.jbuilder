json.extract! pre_student_garbage, :id, :academic_year_id, :student_id, :fee_structure_id, :allocated_fee, :paid_fee, :balance_fee, :created_at, :updated_at
json.url pre_student_garbage_url(pre_student_garbage, format: :json)
